import styled from 'styled-components';

//helpers
import { colors } from '../../helpers/colors';

const Styled = {
  Heading: styled.div`
    h1 {
      color: ${colors.gray};
    }
    h2 {
      color: ${colors.gray};
    }
    h3 {
      color: ${colors.gray};
    }
    h4 {
      color: ${colors.gray};
    }
    h5 {
      color: ${colors.gray};
    }
  `
}

export default Styled;